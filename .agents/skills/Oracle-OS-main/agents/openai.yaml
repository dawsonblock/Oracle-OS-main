interface:
  display_name: "Oracle OS Main"
  short_description: "Repo-specific patterns and workflows for Oracle-OS-main"
  default_prompt: "Use the Oracle-OS-main repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true